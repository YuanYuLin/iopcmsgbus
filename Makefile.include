CROSS_COMPILE = gcc
STRIP = strip
#CROSS_COMPILE = arm-none-linux-gnueabi-gcc

OBJS+=$(filter %.o,$(SRC:.c=.o))

all: $(TARGET)

%.o: %.c
	@rm -f $@
	$(CROSS_COMPILE) $(CFLAGS) $(INCLUDES) -c $< -o $@

version:
	bash gen_version.sh > version.h

$(TARGET): version $(OBJS)
	$(CROSS_COMPILE) $(OBJS) $(LDFLAGS) -o $@
	$(STRIP) $(TARGET)

clean:
	@for i in $(OBJS); do (if test -e "$$i"; then ( rm $$i ); fi ); done
	@rm -f $(TARGET)

